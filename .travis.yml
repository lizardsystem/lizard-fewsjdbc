language: python
python:
  - 2.7
cache:
  directories:
#   - egg
   - $HOME/.local
addons:
  apt:
    packages:
    - libboost-dev
    - libboost-python-dev
    - libmapnik-dev
    - python-mapnik
sudo: false
install:
  - deactivate
  - pip install --user psycopg2 matplotlib
  - python bootstrap.py
  - bin/buildout
script:
  - bin/test
